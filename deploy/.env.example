# Copy this to deploy/.env on the home server (NOT committed to git) and fill
# in a real value. docker-compose.yml reads it automatically when you run
# `docker compose` from this directory.
#
# Generate a long random token, e.g.: openssl rand -hex 32
WATCHTOWER_HTTP_API_TOKEN=
